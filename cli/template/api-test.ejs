/**
* author: <%=file.author%>
* Date: <%=file.date%>
* Name: <%=file.name%>
* Desc: <%=file.desc%>
*/

/* global describe */
/* global before */
/* global beforeEach */
/* global it */
'use strict'

/**
* 引入变量
*/
const paramG = require('../tests/aaa_aBegin')
const assert = paramG.assert
const reqPromise = paramG.reqPromise
const url = paramG.url

/**
* 接口提交测试
*/
describe('接口提交基本测试', async function () {<%content.forEach((item, i) => {%>
  describe('<%=item.url%>  <%=item.method%> <%=item.desc%>', async function () {
    // it 可根据实际需求增加 (默认跳过)
    it.skip('{ 请输入测试结果 }', async function () {
      let r = (await reqPromise({
        method: '<%=(item.method).toUpperCase()%>',
        uri: url + '<%=item.url%>',
        //请根据实际填写以下参数
        headers: {
            
        },
        form: {
          
        },
        qs: {
                    
        },
        json: true
      }))
      // console.log(r)
      assert.strictEqual('请输入实际值', '请输入期望值')
    })
  })<%})%>
})